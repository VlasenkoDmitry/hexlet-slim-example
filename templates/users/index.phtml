<form action="/users" method="get">
    <input type="search" name="user" value="<?= htmlspecialchars($term) ?>">
    <input type="submit" value="Search" >
</form>

<table>
    <?php foreach ($users as $user) : ?>
    <tr>
        <td><?= htmlspecialchars($user) ?></td>
    </tr>
    <?php endforeach; ?>
</table>